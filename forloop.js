for (let oddNumber = 1; oddNumber <=5; oddNumber +=1){
    if (oddNumber % 2){
        console.log(`${oddNumber} is a Odd Number.`)
    }else {
        `${oddNumber}is a Even Number.`
    }
}

for (let evenNumber = 0; evenNumber <=6; evenNumber +=1){
    if (evenNumber % 2 === 1){
        console.log(`${evenNumber} is a Odd Number.`)
    }
    // else {
    //    console.log (`${evenNumber} is a Even Number.`)
    // }
}

for (let evenNumber = 0; evenNumber <=100; evenNumber +=1){
    if (evenNumber % 2 === 1){
        console.log(`${evenNumber} is a Odd Number.`)
    }
    else {
       console.log (`${evenNumber} is a Even Number.`)
    }
}